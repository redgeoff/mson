{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "apollo-cache-inmemory": "^1.1.12",
    "apollo-client": "^2.2.8",
    "apollo-link-http": "^1.5.4",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "events": "^2.0.0",
    "graphql": "^0.13.2",
    "graphql-tag": "^2.9.1",
    "husky": "^0.14.3",
    "lint-staged": "^7.0.0",
    "lodash": "^4.17.5",
    "material-ui": "1.0.0-beta.34",
    "material-ui-icons": "^1.0.0-beta.36",
    "node-fetch": "^2.1.2",
    "prettier": "^1.11.1",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.1.1",
    "sift": "^5.1.0",
    "typeface-roboto": "^0.0.54"
  },
  "lint-staged": {
    "src/**/*.{js,jsx,json,css}": [
      "prettier --single-quote --write",
      "git add"
    ]
  },
  "scripts": {
    "lint": "eslint src",
    "precommit": "lint-staged",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "just-test": "react-scripts test --env=jsdom",
    "test": "npm run lint && npm run just-test",
    "copy-json-rsync": "rsync -avm --include='*.json' -f 'hide,! */' src/mson lib",
    "copy-json": "cd src && find . -name '*.json' -exec cp --parents \\{\\} ../lib \\;",
    "compile": "export NODE_ENV=production && babel src/mson -d lib/mson && npm run copy-json",
    "touch": "find src -exec touch {} \\;"
  }
}
